<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= post.title %> | Bangladesh Blogs</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-6">
  <div class="max-w-3xl mx-auto bg-white p-8 rounded shadow">
    <h1 class="text-3xl font-bold text-green-800 mb-4"><%= post.title %></h1>
    <p class="text-gray-600 italic mb-6"><%= post.summary %></p>
    <div class="text-gray-800 leading-relaxed"><%= post.content %></div>
    <% if (post.source) { %>
      <p class="mt-6 text-sm text-blue-600 hover:text-black cursor-pointer">Source: <%= post.source %></p>
    <% } %>
    <a href="/" class="mt-8 inline-block text-green-700 hover:underline">← Back to home</a>
  </div>
</body>
</html>
